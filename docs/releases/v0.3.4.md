# v0.3.4 - Text Box Submit Fix

Removed the SMAPI-wide input suppression introduced in v0.3.3. It prevented the text-entry menu from seeing `Enter` and `Escape`. Printable characters still remain isolated because the custom menu no longer forwards them to Stardew's base menu handler.
